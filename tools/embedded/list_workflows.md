# list_workflows

List all registered workflows with their names, descriptions, triggers, and step counts.

**Category**: workflow

## Parameters

No parameters required.

## Schema

```json
{
  "name": "list_workflows",
  "description": "List all registered workflows with their names, descriptions, triggers, and step counts.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": []
  }
}
```
